apply plugin: 'kotlin'
apply plugin: 'konan'

konan.targets += ['android_arm32', 'android_arm64', 'iphone']

repositories {
    mavenCentral()
}

dependencies {
    compile "org.jetbrains.kotlin:kotlin-stdlib"
}

konanArtifacts {
    library('kurl')
}